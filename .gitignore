# datos: nunca al repo, se regeneran con el pipeline
data/
*.csv
*.parquet
*.zip
*.xlsx
# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
# secretos y entornos
.env
config/*.local.env
# wheel que se sube a artifacts/ (se regenera con `uv build`)
dist/
# terraform
infra/terraform/.terraform/
*.tfstate
*.tfstate.backup
# spark / airflow locales
spark-warehouse/
metastore_db/
orchestration/logs/

# artefacto de Windows por redirecciones
NUL
